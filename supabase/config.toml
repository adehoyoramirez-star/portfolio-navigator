project_id = "yrirandgftnuvdzatwgc"

[functions.fred-data]
enabled = true
verify_jwt = false
entrypoint = "./functions/fred-data/index.ts"
# Cron schedule require Supabase Pro plan — manual trigger via frontend for now
# schedule = "0 8 * * *"

[functions.yahoo-finance-tactical]
enabled = true
verify_jwt = true
import_map = "./functions/yahoo-finance-tactical/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/yahoo-finance-tactical/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/yahoo-finance-tactical/*.html" ]
